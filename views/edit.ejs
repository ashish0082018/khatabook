<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Hisaab</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class=" mx-6 my-3">
    <div class="flex justify-between mx-6 my-3">  
        <div>Khaatabook</div>
        <div class=" flex gap-3">
         <a class="tracking-tight" href="/">Home</a>
         <a class="tracking-tight" href="/create">Create New Hisaab</a>
     </div>
       
         </div>
         <div class="mt-8 flex flex-col">
            <p class="text-xl font-semibold">You 're editing hisaab of  <span class="text-blue-600 font-bold"> <%= filename.split(".")[0] %> </span> </p>
            <form class="flex flex-col" action="/update/ <%= filename %>" method="post">
            <!-- <input name="title" class="bg-zinc-200 p-2 outline-none w-80 rounded-lg mt-4" type="text" placeholder="Shopping hisaab ,Ghar ka kharcha...">  -->
            <textarea name="content" class="border border-2 w-2/3 mt-4 bg-zinc-100 rounded-lg  outline-none p-1" name="" cols="5" rows="10" placeholder="Daal ,aata,Cheeni..." > <%= filedata %> </textarea>
            <!-- <button class="bg-blue-600 w-fit mt-4 rounded-lg py-2 px-3 text-white leading-tight">Update Hisaab</button> -->
             <input type="submit"> 
        </form>
         </div>

</body>
</html>